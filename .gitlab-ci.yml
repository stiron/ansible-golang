Centos6Test:
  image: "centos:7"
  before_script:
     - yum -y install ansible git 
    - echo localhost > inventory
  script:
    - ansible-playbook --syntax-check -i inventory test.yml
    - ansible-playbook -i inventory test.yml --connection=local -u root --become-user=root
UbuntuTest:
  image: "ubuntu:18.04"
  before_script:
    - apt-get -y update && apt-get -y install ansible git 
    - echo localhost > inventory
   script:
    - ansible-playbook --syntax-check -i inventory test.yml
    - ansible-playbook -i inventory test.yml --connection=local -u root --become-user=root
DebianTest:
  image: "debian:9"
  before_script:
    - apt-get -y update && apt-get -y install ansible git
  script:
    - ansible-playbook --syntax-check -i inventory test.yml
    - ansible-playbook -i inventory test.yml --connection=local -u root --become-user=root
    
